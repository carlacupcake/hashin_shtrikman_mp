effective_props:
  eff_min: "{phase_1} + {a_1} / (1 - {alpha_1} * {a_1})"
  eff_max: "{phase_n} + {a_n} / (1 - {alpha_n} * {a_n})"
  eff_prop: "{mixing_param} * {eff_max}  + (1 - {mixing_param}) * {eff_min}"
  alpha_1: "1/(3 * {phase_1})"
  alpha_n: "1/(3 * {phase_n})"
  a_1_term_i: "{phase_i_vol_frac} / (1/({phase_i} - {phase_1}) + {alpha_1})"
  a_n_term_i: "{phase_i_vol_frac} / (1/({phase_i} - {phase_n}) + {alpha_n})"
  bulk_alpha_1: "3/(3 * {phase_1_bulk} + 4 * {phase_1_shear})"
  bulk_alpha_n: "3/(3 * {phase_n_bulk} + 4 * {phase_n_shear})"
  shear_alpha_1: "3/5 * 1/{phase_1_shear} * ({phase_1_bulk} + {phase_1_shear}) / (3 * {phase_1_bulk} + 4 * {phase_1_shear})"
  shear_alpha_n: "3/5 * 1/{phase_n_shear} * ({phase_n_bulk} + {phase_n_shear}) / (3 * {phase_n_bulk} + 4 * {phase_n_shear})"
concentration_factors:
  cf_1: "1/{phase_1_vol_frac} * (1 - {vf_weighted_sum_cfs})"
  cf_load_i: "1/({n} - 1) * 1/{phase_i_vol_frac} * ({eff_prop} - {phase_1}) / ({phase_i} - {phase_1})"
  cf_response_i: "{phase_i} * {cf_load_i} / {eff_prop}"
  cf_elastic_i: "1/{phase_i_vol_frac} * {phase_i_elastic} / {eff_elastic} * ({eff_elastic} - {phase_1}) / ({phase_i} - {phase_1})"
